import { EpgFocusDirective } from './epg-focus.directive';

describe('EpgFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new EpgFocusDirective();
    expect(directive).toBeTruthy();
  });
});
